# probability-tpk
Dungeons and Dragons fifth edition combat simulation tool employing the monte carlo simulations 

### To run the simulation:
1. Input your characters into characters.json
2. Run a single simulation with the command `python single_run.py`
3. Run a monte carlo simulation by changing n to an acceptable value and using the command `python monte_carlo.py`